"""jp-adopt-etl: DT MySQL → Postgres batch ETL (U9).

Public surface is the ``orchestrator.main`` console entry point. Library
callers can import mappers directly for testing without touching either
database.
"""

__version__ = "0.1.0"
